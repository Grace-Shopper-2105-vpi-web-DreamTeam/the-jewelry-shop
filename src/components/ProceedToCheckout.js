import React from "react";
import { Link } from "react-router-dom";

export default function Checkout({cartItems}) {
    console.log("the cart itmes in checkout", cartItems)
  return (
    <div>
      checkout Page to come
    
    </div>
  );
};